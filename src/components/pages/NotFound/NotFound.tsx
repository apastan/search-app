import React, {FC} from 'react';
import {HomePageLayout} from '../../layouts/HomePageLayout';

export const NotFound: FC = () => {
    return (
        <HomePageLayout>
            NotFound
        </HomePageLayout>
    );
};